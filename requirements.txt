# Astro Runtime includes the following pre-installed providers packages: https://www.astronomer.io/docs/astro/runtime-image-architecture#provider-packages

# Blueprint framework (powers the no-code DAG authoring UI in the Astro IDE)
airflow-blueprint>=0.2.0

# Providers used by these Blueprints that aren't in the base Runtime image.
# (common.sql and standard ship with Astro Runtime.)
apache-airflow-providers-slack
apache-airflow-providers-http

# Common AI provider — powers the LLM/agent Blueprints (llm, ai_agent, etc.).
# Built on Pydantic AI; configure model credentials via pydanticai* connections.
# Pinned to a lower bound so a provider refactor can't silently drop blueprints.
apache-airflow-providers-common-ai>=0.1.0
# Optional provider deps imported at load time by the AI Blueprints:
#   sqlglot + datafusion -> llm_sql_query (SQL generation + AST validation)
sqlglot>=30.0.0
datafusion
# Runtime file-format readers for the llm_file_analysis Blueprint (Parquet/Avro;
# CSV and JSON need nothing extra). Uncomment pypdf / python-docx for PDF/Word.
pyarrow
fastavro
# pypdf
# python-docx
